"""Original-IP Foundry — feature module.

All Foundry-specific code lives here.  The module is only imported when
``settings.AD_FOUNDRY_ENABLED`` is True (see main.py conditional router
registration).
"""
